# ============================================
# Bjorq Asset Wizard — Home Assistant Add-on
# ============================================
# Reference: https://developers.home-assistant.io/docs/add-ons/configuration

name: Bjorq Asset Wizard
version: "0.1.0"
slug: bjorq_asset_wizard
description: "3D asset analysis, optimization, and catalog service for Bjorq"
url: "https://github.com/your-org/bjorq-asset-wizard"

# Supported architectures
arch:
  - amd64
  - aarch64
  # - armv7  # Uncomment if sharp supports armv7 in your build

# Network
ports:
  3500/tcp: 3500
ports_description:
  3500/tcp: "Asset Wizard API"

# Persistent data directory (mapped to /data in container)
# The add-on should use /data/storage for all file operations
map:
  - share:rw
  - media:rw

# User-configurable options (shown in HA UI)
options:
  log_level: info
  max_file_size_mb: 100
  thumbnail_size: 512
  thumbnail_quality: 80
  max_texture_size: 2048
  texture_quality: 85
  job_retention_hours: 24

# Schema for validating options
schema:
  log_level: list(debug|info|warn|error)
  max_file_size_mb: int(10,500)
  thumbnail_size: int(128,1024)
  thumbnail_quality: int(50,100)
  max_texture_size: int(512,4096)
  texture_quality: int(50,100)
  job_retention_hours: int(0,168)

# Resource usage
startup: application
boot: auto

# TODO: Add icon.png (256x256) and logo.png (256x256) for HA add-on store
# icon.png  — Square icon displayed in the add-on store
# logo.png  — Logo displayed on the add-on info page

# Minimum Home Assistant version
homeassistant: "2024.1.0"

# Panel integration (optional — enables iframe in HA sidebar)
# Uncomment when frontend is ready to be embedded:
# panel_icon: mdi:cube-scan
# panel_title: Bjorq Wizard
# ingress: true
# ingress_port: 3500
